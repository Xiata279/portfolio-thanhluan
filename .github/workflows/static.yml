# Deprecated duplicate — dùng deploy-pages.yml (nhánh master)
# Giữ file trống logic tắt để tránh conflict khi Pages source = Actions
name: Deploy static content to Pages (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy-pages.yml instead"
